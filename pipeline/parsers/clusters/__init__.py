"""One `SpecProfile` per numbering cluster.

Add a state by pointing its descriptor at the profile for its scheme — a new file here
is needed only when a state uses a numbering scheme none of the existing profiles cover.
"""
